在新建树的时候设置key 如果fn树 key = fn 如果ln树 key = ln
树的节点内含一个HashMap盒子 储存了所有相同名字的人
在insert person时 new出person 分别设置ID //前提条件没有输入错重复的人 每个人都有独立ID
需要确定有没有相同key的Node(在insert方法中判断)
此时：1. 如果已有含相同key的节点，直接加入上述盒子中
     2. 如果没有此节点，直接创建一个，按正常程序insert一个节点

如果要remove一个人名节点，直接按正常程序remove一个节点

如果要remove一个人，则需要同时在主程序确认操作哪棵树 输入名字ID组成一个Person，并指定key值(同时操作两棵树)
此时：1. 如果找到key，则进入盒子查找相同ID，如果有，则移出盒子
        看盒子空？ 调用上一个程序remove节点：什么都不做
     2. 如果找不到key，则什么都不做

做成动画 在操作旋转的时候把被旋转的两个/三个node高亮
然后按照每个人的新位置做animate

Search for Person(1. search the PersonNode, by name or surname, 2. search the person with id),
if found, bring up new tab?
if not, feedback

graphicsContainer is an AnchorPane setTopAnchor(pn, 100) setLeftAnchor(pn, 100)
